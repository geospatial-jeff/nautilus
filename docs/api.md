# API reference

The public Python API, generated from source docstrings. Only this page is language-specific — after the
Rust port it's regenerated from `rustdoc`; the guides and references don't change.

## `source`

::: nautilus.dsl.source
    options:
      show_root_heading: false

## `Stream`

Every combinator returns a new `Stream`, so a value is reusable and side-effect-free.

::: nautilus.dsl.Stream
    options:
      members_order: source
      show_root_heading: false
